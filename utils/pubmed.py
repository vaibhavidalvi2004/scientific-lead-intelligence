def has_recent_pubmed_paper(author_name: str) -> bool:
    """
    PubMed enrichment placeholder.

    NOTE:
    - Disabled on Streamlit Cloud due to Biopython limitations.
    - Returns False by default.
    - Logic is intentionally modular and can be re-enabled locally.
    """
    return False
